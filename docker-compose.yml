version: '3'
services:
  memory:
    build: ./memory
    ports:
      - "5002:5002"
    volumes:
      - ./memory/data:/data
      - ./scripts:/app/scripts:ro
    env_file:
      - .env
    restart: unless-stopped

version: '3'
services:
  memory:
    build:
      context: ./memory
      network: host
    network_mode: host
    volumes:
      - ./memory/data:/data
      - ./scripts:/app/scripts:ro
    env_file:
      - .env
    restart: unless-stopped

version: '3'
services:
  memory:
    build: ./memory
    ports:
      - "5002:5002"
    volumes:
      - ./memory/data:/data
      - ./scripts:/app/scripts:ro
    env_file:
      - .env
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: unless-stopped

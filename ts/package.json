{
  "name": "mio-memory-ts",
  "version": "0.1.0",
  "private": true,
  "description": "TS-1 strangler ring 0: TypeScript reverse proxy in front of the Python mio-memory server",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}

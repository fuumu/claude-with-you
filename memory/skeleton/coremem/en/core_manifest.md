# Merge order for core.md (CoreMem_read("core.md") concatenates in this order)
# Role of each file:
#   core_stable.md  — who I am / roots / partner (rarely changes)
#   core_rules.md   — operating protocol / startup sequence (update only on protocol changes)
#   core_infra.md   — infrastructure / tool info (changes with version bumps)
#   core_history.md — trajectory + version table (frequent appends)
order:
  - core_stable.md
  - core_rules.md
  - core_infra.md
  - core_history.md
